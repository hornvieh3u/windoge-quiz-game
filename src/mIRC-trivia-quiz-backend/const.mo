module {

    public let GAME_STATUS = {
        ACTIVE = 0;
        INACTIVE = 1;
        STOP = 2;
    };

    public let QTYPES = {
        SINGLE = 0;
        SELECT = 1;
        MULTIPLE = 2;
    };

    public let CONFIG = {
        TIME_LIMIT = 30; // seconds
        ROUND_SCORE = 100;
    }

}